# Simple Addition Program in Rust

This is a simple Rust program that defines an `add` function to perform addition of two integers and includes a test case to verify its correctness.

## Code Explanation

The `add` function takes two `i32` integers as parameters and returns their sum. The `main` function calls the `add` function with the values 5 and 3, and prints the result.